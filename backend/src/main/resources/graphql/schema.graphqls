# Custom scalars
scalar DateTime
scalar Date
scalar JSON

type Query {
    hello: String!
    reports: [Report!]!
}

type Report {
    id: ID!
    title: String!
    description: String
    createdAt: DateTime
}
