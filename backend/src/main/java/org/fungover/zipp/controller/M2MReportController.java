package org.fungover.zipp.controller;

/**
 * Before adding stuff to this we need to add graphQL or gRPC in our pom.xml
 *
 * This is the actual Machine-API. This provides the m2m client a dedicated endpoint.
 * We have to secure it in SecurityConfig--> Authentication.
 * The data should be accessible via a dedicated endpoint exposed either through GraphQL or gRPC.
 * Beware not to use REST that we are so used to
 *
 **/

public class M2MReportController {
}
